---
"@inlang/cli": patch
---

Remove the hosted RPC fallback for `machine translate`. Users must set `INLANG_GOOGLE_TRANSLATE_API_KEY` with their own Google Cloud Translation API key before running machine translation.
